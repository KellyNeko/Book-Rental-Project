{# Navigation bar #}
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="#">LocaLivres</a>
    <div class="collapse navbar-collapse">
        <div class="navbar-nav">
            <a class="nav-item nav-link home-link" href="{{ path('app_book') }}">Accueil</a>
            {% if app.user %}
                {% for role in app.user.roles %}
                    {% if role == "ROLE_ADMIN" %}
                        <a class="nav-item nav-link user-list-link" href="{{ path('admin') }}">Liste des utilisateurs</a>
                        <a class="nav-item nav-link user-list-link" href="{{ path('admin_renting_list') }}">Liste des locations</a>
                    {% endif %}
                {% endfor %}
                <a class="nav-item nav-link free-books-link" href="{{ path('book_list') }}">Livres disponibles</a>
                <a class="nav-item nav-link my-books-link" href="{{ path('user_book_list') }}">Mes Livres</a>
                <a class="nav-item nav-link edit-user-link" href="{{ path('app_user_crud_edit', {'id': app.user.id}) }}">Modifier son profil</a>
                <a class="nav-item nav-link disconnect-link" href="{{ path('app_logout') }}">Se déconnecter</a>
            {% else %}
                <a class="nav-item nav-link sign-up-link" href="{{ path('app_user_crud_new') }}">S'inscrire</a>
                <a class="nav-item nav-link connect-link" href="{{ path('app_login') }}">Se connecter</a>
            {% endif %}
        </div>
    </div>
</nav>