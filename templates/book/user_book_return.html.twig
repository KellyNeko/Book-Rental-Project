{% extends 'base.html.twig' %}

{# Show details of a selected book to be returned #}
{% block body %}
    {% include '_components/nav.html.twig' %}
    <h1>{{book.reference}}</h1>
    <h2 class="text-center">De : {{book.author.firstname ~" "~ book.author.lastname}}</h2>
    {% for book_category in book_categories %} 
        <h3 class="text-center blockquote-footer">{{book_category.category.label}}</h3>
    {% endfor %}
    <div class="text-center">
        <p>Voulez-vous rendre ce livre ?</p>
        <a href="{{ path('book_return', {'id': book.id}) }}" class="btn btn-danger">Retourner le livre</a>
        <a href="{{ path('user_book_list') }}" class="btn btn-outline-danger">Retour à la liste</a>
    </div>
{% endblock %}